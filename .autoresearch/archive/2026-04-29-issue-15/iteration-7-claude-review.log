Now I have a complete picture of the codebase. Let me write the review report.

---

## 审核报告

### Issue
- 编号: #15
- 标题: #15 胜负判定 — 游戏结束条件检查
- 子任务: T-003 - 玩家端处理 WIN 消息并用伪装格式显示结果
- 迭代次数: 1

---

### 总体评价

**评分: 10/100**

T-003 子任务的核心功能**完全未实现**。`player.go` 中没有 `MsgWin` 的处理分支，`display.go` 中没有 `ShowGameResult` 方法，也没有任何相关测试。WIN 消息目前落入 `default` 分支，以原始格式 `WIN winner|playerStates|civilianWord|undercoverWord` 显示，完全不符合伪装格式要求。

---

### 严重问题 (必须修复)

#### 问题 1: handleMessage 缺少 MsgWin 处理

**位置**: `cmd/doubletake/player.go:164-244`

**问题描述**:
`handleMessage` 的 `switch msg.Type` 中没有 `case game.MsgWin:` 分支。当裁判端广播 WIN 消息时（见 `judge.go:217`），玩家端会落入 `default` 分支，以原始协议格式显示数据，无法正确解析胜负结果。

**当前行为**:
```go
// player.go:241-243 (default 分支)
default:
    disp.Data("00", fmt.Sprintf("%s %s", msg.Type, msg.Payload))
```

WIN 消息到达时显示为：
```
[DATA] [node-00] WIN Civilian|Alice:Civilian:1,Bob:Undercover:0,...|苹果|香蕉
```

**预期行为**:
应解析 WIN payload 并以伪装格式显示游戏结果，包括胜负方、各玩家角色（中文）、双方词语，然后返回 `false` 退出消息循环。

**建议修复**:
在 `handleMessage` 的 `switch` 中添加 `case game.MsgWin:` 分支，调用新增的 `handleWinMsg` 函数解析 payload 并显示结果，然后 `return false`。

---

#### 问题 2: client/display.go 缺少 ShowGameResult 方法

**位置**: `client/display.go`

**问题描述**:
任务要求在 `client/display.go` 中新增 `ShowGameResult` 方法封装结果展示，但该方法不存在。当前 `Display` 只有 `PrintStartup`、`Prompt`、`Info`、`Warn`、`Data`、`ListPlayers` 方法。

**建议修复**:
在 `display.go` 中新增 `ShowGameResult` 方法，接受胜负方、玩家角色列表、双方词语等参数，以伪装格式（如 `[DATA] [node-XX]` 前缀）输出游戏结果。

---

#### 问题 3: 无 WIN 消息处理的测试

**位置**: `cmd/doubletake/player_test.go`, `client/display_test.go`

**问题描述**:
没有任何测试覆盖 WIN 消息的接收、解析和显示。现有测试覆盖了 JOIN、ROLE、DESC、VOTE、RESULT 等消息类型，但缺少 WIN 场景。

**建议修复**:
至少需要以下测试：
1. `TestRunPlayer_ReceivesWin_CivilianWins` — 平民胜场景
2. `TestRunPlayer_ReceivesWin_UndercoverWins` — 卧底胜场景
3. `TestShowGameResult` — Display 方法的单元测试
4. 验证 WIN 消息后客户端正常退出（exit code 0）

---

### 一般问题 (建议修复)

#### 问题 4: WIN payload 解析应考虑边界情况

**位置**: N/A（尚未实现）

**问题描述**:
`buildWinPayload` 生成的格式为 `"winner|playerStates|civilianWord|undercoverWord"`，其中 `playerStates` 使用逗号分隔，每个 playerState 使用冒号分隔（`name:role:alive`）。解析时需注意：
- payload 中有 4 个 `|` 分隔的部分
- playerStates 可能包含多个玩家
- 需要处理玩家名或词语中包含特殊字符的极端情况

**建议修复**:
使用 `strings.SplitN(payload, "|", 4)` 分割，然后对 playerStates 部分使用 `strings.Split(states, ",")` 进一步解析。

---

### 建议改进 (可选)

- 可考虑在 `handleWinMsg` 中对未知角色名做 fallback 处理（如 `roleDisplayNames` 映射不到时使用原始英文名）
- WIN 消息后退出应使用 `return false`（当前约定 `false` 表示退出消息循环），退出码应为 0（正常结束）

---

### 验收条件检查

| 验收条件 | 状态 | 说明 |
|---------|------|------|
| 玩家收到 WIN 消息后正确解析并显示游戏结果 | 未实现 | 无 MsgWin 处理分支 |
| 结果显示包含胜负方、各玩家角色（中文）、双方词语 | 未实现 | 无 ShowGameResult 方法 |
| 输出使用伪装格式（与现有 Display 风格一致） | 未实现 | 落入 default 分支显示原始格式 |
| 显示结果后客户端正常退出 | 未实现 | 无 return false 逻辑 |
| 编译通过，go vet 无错误 | 通过 | go build 和 go vet 均通过 |

---

### 审核结论

- [x] 需要修改 - T-003 子任务的核心功能完全未实现

**下一步行动**:
1. 在 `client/display.go` 中新增 `ShowGameResult` 方法
2. 在 `player.go` 的 `handleMessage` 中新增 `case game.MsgWin:` 分支和 `handleWinMsg` 辅助函数
3. 在 `player_test.go` 中新增 WIN 消息处理的集成测试
4. 在 `display_test.go` 中新增 `ShowGameResult` 的单元测试
5. 确保 `go build`、`go vet`、`go test` 全部通过

---

## Learnings

- **模式**: 项目使用 `handleXxxMsg` 辅助函数模式处理各类消息（如 `handleRoundMsg`、`handleDescMsg`、`handleVoteMsg`、`handlePKStartMsg` 等），WIN 消息应遵循相同模式
- **模式**: `Display` 的伪装输出统一使用 `disp.Data("00", ...)` 格式，stealth 模式下去掉 `[DATA]` 前缀
- **模式**: `handleMessage` 返回 `false` 表示退出消息循环，`RunPlayer` 返回退出码 0 表示正常退出
- **经验**: `buildWinPayload` 的格式为 `"winner|playerStates|civilianWord|undercoverWord"`，其中 playerStates 是 `name:role:alive` 逗号分隔列表，解析时需要用 `SplitN(payload, "|", 4)` 而非 `SplitN(payload, "|", 2)`
