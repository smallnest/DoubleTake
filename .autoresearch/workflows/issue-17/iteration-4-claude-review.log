I now have a complete picture of the implementation. Let me write the review report.

## 审核报告

### Issue
- 编号: #17
- 子任务: T-002 — 掉线玩家回合 60 秒超时自动跳过
- 迭代次数: 4

---

### 总体评价

**评分: 87/100**

实现质量良好，核心功能完整。`descriptionPhase`、`votingPhase`、`pkPhase` 三个循环均正确实现了掉线超时跳过逻辑，模式一致、定时器管理正确。编译通过、go vet 无警告、全部测试通过。主要扣分在测试覆盖方面：缺少 PK 阶段超时测试和 OnDisconnect 事件重启定时器的测试。

---

### 验收条件检查

| # | 条件 | 状态 | 说明 |
|---|------|------|------|
| 1 | 掉线玩家轮到发言时，等待 60 秒后自动跳过 | ✅ | `judge.go:488-555` — `descriptionPhase` 中检测掉线、启动 timer、超时后 `SkipCurrent()` |
| 2 | 掉线玩家轮到投票时，等待 60 秒后视为弃票 | ✅ | `judge.go:598-661` — `votingPhase` 中同样模式，超时后 `round.SkipCurrent()` |
| 3 | 超时跳过后自动将 TURN 发送给下一位玩家 | ✅ | 超时后广播 `MsgTurn` 给 `round.CurrentSpeaker()`/`round.CurrentVoter()` |
| 4 | 掉线玩家在超时前重连可继续操作 | ✅ | 重连后 DESC/VOTE 通过 `OnDescMsg`/`OnVoteMsg` 正常处理，`stopTimer` 取消超时 |
| 5 | 编译通过，无 lint 错误 | ✅ | `go build ./...` 和 `go vet ./...` 均通过 |
| 6 | 新增测试验证超时跳过逻辑 | ✅ | 6 个新测试覆盖超时跳过和重连场景 |

---

### 严重问题 (必须修复)

无。

---

### 一般问题 (建议修复)

#### 问题 1: 缺少 PK 阶段超时跳过测试

**位置**: `cmd/doubletake/judge_test.go`

**问题描述**:
`pkPhase`（`judge.go:711-934`）的描述子循环和投票子循环均实现了超时跳过逻辑，但测试文件中没有任何 PK 阶段的超时跳过集成测试。验收条件要求"新增测试验证超时跳过逻辑"，PK 阶段是 `pkPhase` 中的独立循环，应有对应测试。

**建议修复**:
新增 `TestPKPhase_DisconnectedPlayerTimeout_Skip` 测试，覆盖 PK 描述阶段的超时跳过。

#### 问题 2: 缺少 OnDisconnect 事件重启定时器的测试

**位置**: `cmd/doubletake/judge_test.go`

**问题描述**:
当前超时测试（如 `TestDescriptionPhase_DisconnectedPlayerTimeout_Skip`）通过在定时器启动前关闭连接来触发掉线，测试的是"玩家一开始就是掉线状态"的场景。但 `judge.go:538-544` 和 `judge.go:643-649` 中的 `case disc := <-srv.OnDisconnect:` 处理的是"玩家在回合进行中掉线"的场景（定时器已启动，玩家后来才断开）。这个路径没有被测试覆盖。

**建议修复**:
新增测试：让玩家先连接（定时器不启动），然后在回合进行中关闭连接，验证 OnDisconnect 事件触发定时器重启并最终超时跳过。

#### 问题 3: 包级变量 `disconnectTimeout` 并发安全

**位置**: `cmd/doubletake/judge.go:40`

**问题描述**:
`disconnectTimeout` 是包级 `var`，测试中直接修改（`disconnectTimeout = 50 * time.Millisecond`）。虽然当前 `go test` 默认串行执行测试，但如果未来使用 `t.Parallel()`，多测试同时修改该变量会导致竞态。

**建议修复**:
可接受当前实现（测试不并行），但建议在测试中使用 `defer` 恢复原值（已做到）。长期可考虑通过参数传递超时值。

---

### 建议改进 (可选)

- `stopTimer` 函数的 channel drain 使用 `select default` 模式，这是正确的。但如果将来需要更精确的定时器管理，可以考虑使用 `time.NewTimer` 后立即 `Reset` 的模式。
- 超时跳过后，judge 端日志使用中文（"超时未发言（已掉线），跳过"），建议后续统一日志语言。

---

### 审核结论

- [x] 通过 - 代码质量达标，可以进入人工审核
- [ ] 需要修改 - 存在必须修复的问题

**下一步行动**:
建议补充 PK 阶段超时跳过测试和 OnDisconnect 事件重启定时器的测试，以提高测试覆盖率。核心功能实现正确，不影响主流程。

---

## Learnings

- **模式**: `judge.go` 中的三个 phase 循环（description、voting、pk）均使用统一的 `select` 多路复用模式：消息 channel + OnDisconnect channel + timeoutCh。`isPlayerDisconnected` 在循环顶部检查，仅掉线玩家启动 timer。已连接玩家的 `timeoutCh` 为 nil，select 自动忽略该 case。
- **踩坑**: `stopTimer` 必须先 `Stop()` 再 drain channel。Go 的 `Timer.Stop()` 在 timer 已触发时返回 false，但 channel 上的值需要手动 drain，否则下次 select 可能立即触发旧的 timeout。
- **经验**: 集成测试中覆盖断线场景的标准模式：读取初始消息 → 关闭连接 → 从另一玩家发消息触发循环重入 → 等待超时输出 → 验证 TURN 广播。重连测试则额外通过新 TCP 连接发 RECONNECT + 有效消息，验证不触发超时。
