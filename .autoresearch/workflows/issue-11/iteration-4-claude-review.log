我现在对情况有了全面的了解。这是我的评审意见。

---

## 审核报告

### 问题
- 编号: #11
- 标题: 词语输入与分发
- 子任务: T-002 - 服务端实现按玩家名私密发送 ROLE 消息
- 迭代次数: 4 (审核)

---

### 总体评价

**评分: 88/100**

T-002 的实现完整且质量良好。`SendToPlayer` 方法在 `server/server.go` 中正确实现，`sendRoleToPlayers` 在 `cmd/doubletake/judge.go` 中正确调用，单元测试和集成测试均通过。所有验收条件均已满足。

---

### 验收条件逐项检查

| # | 验收条件 | 状态 |
|---|----------|------|
| 1 | Server 新增 `SendToPlayer(name string, msg Message)` 方法 | **通过** - `server.go:206-219` |
| 2 | 玩家名不存在时返回明确错误 | **通过** - 返回 `fmt.Errorf("player %s not found", name)` |
| 3 | judge.go 中角色分配完成后遍历玩家私密发送 ROLE | **通过** - `judge.go:264-285` `sendRoleToPlayers` |
| 4 | 平民/卧底/白板消息格式正确 | **通过** - Civilian→`ROLE|Civilian|词语`, Undercover→`ROLE|Undercover|词语`, Blank→`ROLE|Blank|你是白板` |
| 5 | 消息仅发送给目标玩家 | **通过** - `TestSendToPlayer_OnlyTargetReceives` 验证 |
| 6 | SendToPlayer 有对应单元测试 | **通过** - 3个测试：Success, NotFound, OnlyTargetReceives |
| 7 | go build/vet 无错误 | **通过** |

---

### 正确性 (35%): 95/100

**优点**:
- `SendToPlayer` 持锁遍历 connections map 查找玩家名，逻辑正确
- 发送失败时返回包装错误 (`fmt.Errorf("send to player %s: %w", name, err)`)，保留错误链
- `sendRoleToPlayers` 中 switch 覆盖三种角色，default 无遗漏（go 编译器会检查）
- 空白玩家收到 "你是白板" 而非空字符串，符合需求

**无问题**: 功能完整，边界情况处理得当。

---

### 测试质量 (25%): 88/100

**优点**:
- `TestSendToPlayer_Success` - 验证正常发送和消息内容
- `TestSendToPlayer_NotFound` - 验证玩家不存在时的错误返回
- `TestSendToPlayer_OnlyTargetReceives` - 验证私密性（其他玩家收不到）
- `TestWaitingPhase_StartConfirmed` - 集成测试验证每个玩家收到 ROLE + READY

**建议改进**:
- 缺少对已断开玩家的 `SendToPlayer` 测试（send 失败路径）
- `sendRoleToPlayers` 函数本身无独立单元测试（仅通过集成测试间接覆盖）

---

### 代码质量 (20%): 90/100

**优点**:
- `SendToPlayer` 签名清晰，注释充分
- `sendRoleToPlayers` 函数抽取合理，职责单一
- 命名遵循项目约定，与 Broadcast/Send 风格一致
- 持锁发送与 Broadcast 模式一致（最大连接数 ≤ 10，阻塞风险可忽略，`server/CLAUDE.md` 已记录）

**建议改进**:
- `sendRoleToPlayers` 中 `roleName` 可用 `p.Role.String()` 替代手动 switch，减少重复（`Role.String()` 已实现），但当前写法更直观，可接受

---

### 安全性 (10%): 95/100

- 玩家名来自 `handleJoin` 的已验证输入（非空、无重复）
- 无注入风险，消息通过 `game.Encode` 编码
- 私密消息确保只有目标玩家收到，无泄露风险

---

### 性能 (10%): 85/100

- `SendToPlayer` 使用 O(n) 遍历 connections map（`server/CLAUDE.md` 已记录）
- 对于最大 10 个连接，性能完全可接受
- 如果未来连接数增加，可考虑添加 `name→*Player` 反向映射

---

### 审核结论

- [x] **通过** - 代码质量达标，可以进入人工审核

**下一步行动**:
T-002 实现完整，所有验收条件满足，测试通过。可以继续 T-003 的实现。

---

## 学习经验

- **模式**: `sendRoleToPlayers` 将角色分发逻辑从 `RunJudge` 抽取为独立函数，遵循单一职责原则，便于测试和维护
- **经验**: `SendToPlayer` 的 O(n) 查找在当前场景（≤10 连接）可接受，但如果后续需要更高性能，可以添加 `names` map 扩展为 `name→*Player` 反向索引
- **发现**: `Role.String()` 方法已实现（返回 "Civilian"/"Undercover"/"Blank"），`sendRoleToPlayers` 中的手动 switch 可在未来重构时利用

无需更新 `CLAUDE.md` 文件 — `server/CLAUDE.md` 和 `cmd/doubletake/CLAUDE.md` 已包含 T-002 的相关知识（SendToPlayer 方法、sendRoleToPlayers 流程等）。
