## 实现总结

T-002 已完整实现，验收条件逐项检查：

| 验收条件 | 状态 |
|---|---|
| 解析 `--role`，仅接受 judge/player，其他值报错 | 通过 |
| 解析 `--port`，默认值 8127 | 通过 |
| `--role judge` 输出裁判模式提示信息 | 通过 |
| `--role player` 输出玩家模式提示信息 | 通过 |
| 无参数或 `--help` 显示用法说明 | 通过 |
| `go build` 编译通过 | 通过 |
| 单元测试覆盖参数解析逻辑（12 个测试用例） | 通过 |
| `go vet ./...` 无警告 | 通过 |

**通过率: 8/8**

关键设计决策：
- `run(stdout, stderr io.Writer, args []string) int` — 将核心逻辑从 `main()` 提取为可测试函数，通过 `io.Writer` 注入使测试能验证输出内容
- 手动解析 flag 而非使用 `flag` 包 — 避免 `flag.Parse()` 内部调用 `os.Exit` 导致测试无法捕获

## Learnings

- **模式**: Go CLI 程序应将核心逻辑提取为 `run(stdout, stderr, args) int`，通过依赖注入使所有路径都可测试
- **踩坑**: `flag.Parse()` 会在 `--help` 时调用 `os.Exit(0)`、参数错误时调用 `os.Exit(2)`，导致测试无法捕获这些分支。手动解析 flag 是更可控的方案
- **经验**: 测试应同时验证退出码和输出内容，而非仅验证退出码
