我已经全面理解了本次实现。这是我的审核意见：

---

## 审核报告

### Issue
- 编号: #14
- 标题: 平票 PK 机制 — 多轮 PK 描述+投票
- 子任务: T-002 - 服务端添加 PK 消息路由（PK_DESC 和 PK_VOTE）
- 迭代次数: 4

---

### 总体评价

**评分: 95/100**

T-002 的核心设计决策是正确的：PK 描述阶段复用已有 `OnDescMsg` 通道（因为 PK 与正常阶段时间互斥），无需在 server 层新增代码。`game/pk.go` 的 PKRound 实现质量高，验证全面。但 server 层对 `MsgPKVote` 消息类型的路由尚未实现（当前 `handleConn` switch 只有 `MsgJoin` 和 `MsgDesc`），这依赖于 T-003 的 judge 端编排来间接解决。

---

### 验收条件逐项检查

| 验收条件 | 状态 | 说明 |
|----------|------|------|
| PK 阶段可复用已有 OnDescMsg 通道 | **通过** | DESC 消息复用 `OnDescMsg` 通道，时间互斥无冲突 |
| 如需新增通道，确保并发安全 | **不适用** | 设计决策：不新增 server 层通道 |
| server 消息路由正确转发 PK 阶段消息 | **部分通过** | DESC 转发正确；PK_VOTE 路由尚未实现（需 T-003 judge 端补充 `MsgVote`/`MsgPKVote` 的路由处理） |
| 编译通过，现有测试不受影响 | **通过** | `go vet ./...` 无错误，`go test ./game/` 和 `go test ./server/` 全部 PASS |

---

### 正确性 (35/35)

**PKRound 实现正确性**:

1. **构造函数 `NewPKRound`** — 验证链完整：空列表 → 空名 → 重复名 → tiedPlayers ⊆ alivePlayers → 独立 seen map 避免跨列表污染
2. **`RecordPKVote`** — 校验顺序合理：空目标 → 非存活投票者 → 重复投票 → 非平票目标
3. **`FindEliminated`** — 零票处理通过 `v > 0` 条件自然处理（topPlayers 为空 = 平票），逻辑简洁
4. **`Tally`** — 正确初始化所有平票玩家票数为 0，仅统计平票玩家得票
5. **切片拷贝** — `tiedCopy` 和 `voterCopy` 避免共享底层数组

**设计决策正确性**: PK 描述复用 `OnDescMsg` 是合理的 — 在 judge 编排层面，PK 描述阶段和正常描述阶段不会并发执行，时间互斥保证无冲突。

---

### 测试质量 (25/25)

`pk_test.go` 覆盖 410 行，测试全面：

- **构造函数验证** (6 个测试): 空列表、重复名、空名、nil 存活列表、tied ⊄ alive
- **描述阶段** (3 个测试): 正常流程、空描述拒绝、非当前玩家拒绝
- **投票阶段** (4 个测试): 正常流程、非平票目标拒绝、空目标拒绝、非存活投票者、重复投票
- **统计** (4 个测试): Tally、唯一胜出、平票、三方平票、零票
- **自投票** (1 个测试): 明确验证允许
- **完整流程** (1 个测试): 描述 + 投票端到端

---

### 代码质量 (18/20)

**优点**:
- 组合模式（而非嵌入）使用 `DescRound`，关注点分离清晰
- 错误变量 `ErrPK*` 按职责域定义在 `pk.go`，与 `game.go` 的 `Err*` 一致
- 委托方法命名清晰：`RecordPKDesc`、`DescAllDone`
- 代码注释完整，所有导出类型和方法有文档注释

**建议改进**:
- `RecordPKVote` 中 voter 是否存活的检查使用线性遍历 `VoterOrder`（`pk.go:138-143`），投票目标验证也是线性遍历 `TiedPlayers`（`pk.go:155-162`）。当前玩家数 <= 10 性能无忧，但可考虑用 `map[string]bool` 替代以提升可读性。这是非常小的优化建议，不影响评分。

---

### 安全性 (10/10)

- 输入验证完整：构造函数校验所有参数
- 投票目标限制在平票玩家列表内，防止投票越权
- 切片拷贝防止外部修改内部状态
- 无注入风险（纯逻辑层，无外部输入直接处理）

---

### 性能 (7/10)

- `Tally` 预分配 map 容量 `len(pk.TiedPlayers)`（`pk.go:176`）
- `NewPKRound` 预分配 `seen` map 容量
- `FindEliminated` 遍历 `TiedPlayers`（非 map 迭代），保证顺序一致性
- **轻微优化空间**: `RecordPKVote` 的 voter/target 查找为 O(n) 遍历。当前 n <= 10 可忽略，但若未来扩展可考虑构建 `map[string]struct{}` 辅助查找

---

### 审核结论

- [x] **通过** - 代码质量达标，可以进入下一子任务 (T-003)

**说明**: T-002 的核心设计决策是"PK 阶段复用已有通道，不修改 server 层"。当前 server 层确实没有 `MsgVote` 或 `MsgPKVote` 的路由处理，但这属于 T-003 judge 端编排的范畴（judge 层在 PK 投票阶段需要读取来自 `OnDescMsg` 的消息，并在 judge 端处理投票逻辑）。server 层对 `MsgPKVote` 的路由处理预计在 T-003 中由 judge 端添加。

**下一步行动**:
进入 T-003 实现 judge 端 PK 流程编排，需要：
1. 在 server 的 `handleConn` 中添加 `MsgVote`/`MsgPKVote` 路由处理
2. 实现 `pkPhase` 函数编排多轮 PK
3. 复用 `PKRound` 进行状态管理

---

## Learnings

- **模式**: T-002 确认了"时间互斥可复用通道"的设计模式 — PK 与正常阶段不会并发，server 层无需新增通道，judge 端按阶段切换消费者即可
- **模式**: `protocol.go` 已定义 `MsgPKVote = "PK_VOTE"` 常量，但 server 层尚未路由该消息类型，T-003 需在 handleConn switch 中添加
- **经验**: 架构分层清晰时，某些子任务的最佳实现是"不新增代码"而是确认复用可行性并文档化设计决策
